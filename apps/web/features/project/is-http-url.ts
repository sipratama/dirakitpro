/** Well-formed http(s) URL check (PRJ-002: live URL and screenshot URL validation). */
export function isHttpUrl(value: string): boolean {
  try {
    const url = new URL(value);
    return url.protocol === "http:" || url.protocol === "https:";
  } catch {
    return false;
  }
}
